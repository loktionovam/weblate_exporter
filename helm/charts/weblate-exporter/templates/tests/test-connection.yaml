apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weblate-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "weblate-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weblate-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
